#include <math.h>

int add(int x, int y) {
    return x + y;
}

int sous(int x, int y) {
    return x - y;
}

int mul(int x, int y) {
    return x * y;
}

int div(int x, int y) {
    return x / y;
}

int res(int x, int y) {
    return x % y;
}

double racine(double x) {
    return sqrt(x);
}